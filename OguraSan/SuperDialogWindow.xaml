<Window x:Class="SuperDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OguraSan"
        mc:Ignorable="d"
        x:Name="SuperDialogWindow"
        Title="フォント設定" Height="225" Width="800" WindowStartupLocation="CenterOwner" FontFamily="BIZ UDPGothic" FontSize="16">
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border x:Name="SuperSuperSuperBorder">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="SuperListBox" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,10,10"/>
                <Slider x:Name="SuperSlider" Grid.Column="1" Maximum="100" Minimum="1" VerticalAlignment="Center" ValueChanged="SuperSlider_ValueChanged"/>
                <TextBox x:Name="SuperFontSize" Grid.Row="1" Grid.Column="1" Text="{Binding ElementName=SuperSlider, Path=Value, Mode=TwoWay}"/>
                <!--<TextBlock x:Name="Sample" Grid.Row="2" Grid.ColumnSpan="2" FontFamily="{Binding ElementName=SuperListBox, Path=SelectedItem, Mode=OneWay}" FontSize="{Binding ElementName=SuperSlider, Path=Value, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center">このように表示されます．</TextBlock>-->
            </Grid>
        </Border>
        <Button Grid.Row="1" x:Name="SuperSetButton" Content="確定" HorizontalAlignment="Right" Padding="5,5,5,5"/>
    </Grid>
</Window>
